apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: generator-api-ingress
  namespace: scaffoldops
spec:
  ingressClassName: kong
  rules:
    - host: generator-api.local
      http:
        paths:
          - path: /generation-requests
            pathType: Prefix
            backend:
              service:
                name: generator-api-service
                port:
                  number: 80
