apiVersion: v1
kind: PersistentVolume
metadata:
  name: generator-api-postgres-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: "/mnt/data/generator-api-postgres"
